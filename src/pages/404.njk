---
title: Whoops!
layout: layouts/page.njk
permalink: /404.html
hideTagsList: true
eleventyExcludeFromCollections: true
---

<h2>Content not found.</h2>

Go <a href="{{ '/' | url }}">home</a> or browse the archive

{% for year, yearPosts in collections.postsByYear %}
  <h2>{{ year }}</h2>
  <ul>
    {% for post in yearPosts | reverse %}
      <li>{{ post.date | readableDate }} | <a href="{{ post.url | url }}">{{ post.data.title }}</a></li>
    {% endfor %}
  </ul>
{% endfor %}